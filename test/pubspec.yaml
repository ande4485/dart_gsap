name: dart_gsap_test
description: A pure client-side app without ssr.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  jaspr: ^0.9.2
  
  dart_gsap:
    path: ../dart_gsap

dev_dependencies:
  build_runner: ^2.4.0
  build_web_compilers: ^4.0.4
  jaspr_test: ^0.9.2
  #jaspr_builder: ^0.9.0
  lints: ^2.1.0

jaspr:
  uses-ssr: false
